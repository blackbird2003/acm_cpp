//
// Created by vv123 on 2022/3/6.
//
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    while (cin >> n && n) {
        int score[2] = {0, 0};
        string s, st[2];
        for (int i = 0; i < n; i++) {
            getline(cin, s);
            if (s.substr(s.size() - 7, s.size() - 5) == "no") {
                st[i % 2].push_back('')
            }

        }
    }

    return 0;
}


